let valores = [8, 1, 7, 4, 2, 9]
    console.log(valores) // forma de mostrar padrão

for(let pos=0; pos < valores.length; pos++) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`) // forma de mostrar com formatação

}